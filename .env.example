# ============================================
# CONFIGURACIÓN DE DESARROLLO - VCAJA BACKEND
# ============================================
# Copy this file to .env and fill values.
# ⚠️  NEVER commit real secrets to the repository ⚠️

# Base de datos MongoDB
MONGODB_URI=mongodb://localhost:27017/registro_db

# Puerto del servidor (se auto-ajusta si está ocupado)
PORT=5000

# JWT Secret (generar una clave fuerte de al menos 32 caracteres)
# Usar: openssl rand -hex 32
JWT_SECRET=your_super_secure_jwt_secret_key_here_min_32_chars

# Clerk Configuration (obtener desde Clerk Dashboard)
# ⚠️ IMPORTANTE: Regenerar las claves si las anteriores fueron comprometidas
CLERK_SECRET_KEY=sk_test_your_new_clerk_secret_key_here
CLERK_ISSUER=https://your-clerk-domain.clerk.accounts.dev

# CORS origin (URL del frontend)
CORS_ORIGIN=http://localhost:5173

# Entorno de desarrollo
NODE_ENV=development

# Opciones adicionales
# SKIP_DB=false  # Para testing sin BD
# LOG_LEVEL=debug  # Nivel de logging
