# Copy this file to .env and fill values. Do NOT commit secrets.
MONGODB_URI=mongodb://localhost:27017/registro_db
PORT=5000
JWT_SECRET=tu_clave_secreta_aqui

# Clerk configuration (required if you use Clerk for auth)
# Set CLERK_SECRET_KEY to your Clerk secret key and CLERK_ISSUER to the issuer from Clerk dashboard
CLERK_SECRET_KEY=sk_test_your_secret_key_here
CLERK_ISSUER=https://well-marmot-74.clerk.accounts.dev

# CORS origin (set to your frontend URL in production)
CORS_ORIGIN=http://localhost:5173

# Optional: set SKIP_DB=true to start without connecting to Mongo (useful for health checks or smoke runs)
# SKIP_DB=false
